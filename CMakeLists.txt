cmake_minimum_required(VERSION 2.8.3)
project(timed_roslaunch)
find_package(catkin REQUIRED COMPONENTS
  #  rospy
)

# These are for launch test
#find_package(roslaunch)
#roslaunch_add_file_check(test/timed_roslaunch.test.xml)
